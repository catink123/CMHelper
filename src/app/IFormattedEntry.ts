export default interface IFormattedEntry {
    championId: number,
    name: string,
    championLevel: number,
    championPoints: number,
    chestGranted: number,
    tokensEarned: number,
    imageURL: string
}